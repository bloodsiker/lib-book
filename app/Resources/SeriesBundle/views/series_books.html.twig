<div class="col-cont">
    <div class="container">

        <div class="section-title clearfix">
            <h2>Книги из серии <span>{{ series.title }} ({{ series.getBooks().count() }})</span></h2>
        </div>

        <div class="movies clearfix grid grid-list">

            {{
            sonata_block_render(
                { 'type': 'book.block.list' },
                {
                    'items_count' : 15,
                    'page': app.request.get('page') ? app.request.get('page') : 1,
                    'series': series,
                }
            )
            }}

        </div>
    </div>

</div>