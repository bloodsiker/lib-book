{# index.twig #}
{% extends "AppBundle:default/layout:layout_sidebar.html.twig" %}

{% block page %}
    {% if content is defined %}
        {{ content|raw }}
    {% endif %}
    {{ sonata_page_render_container('content') }}
{% endblock %}

{% block page_bottom %}
    {{ sonata_page_render_container('bottom_content') }}
{% endblock %}
