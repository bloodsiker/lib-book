{# index.twig #}
{% block page %}
    {%  if content is defined %}
        {{ content|raw }}
    {% endif %}

    {{ sonata_block_render({ 'type': 'sonata.block.service.html_page' }, {'path': page.url}) }}


{% endblock %}
