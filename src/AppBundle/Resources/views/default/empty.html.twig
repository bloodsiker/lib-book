{# index.twig #}
{% extends "AppBundle:default/layout:layout_empty.html.twig" %}

{% block page %}
    {%  if content is defined %}
        {{ content|raw }}
    {% endif %}
    {{ sonata_page_render_container('content') }}
{% endblock %}
