{% extends admin.getTemplate('base_list_field') %}

{% block field %}
    {% set v = asset('bundles/admin/images/preview_placeholder.png') %}
    {% set image_preview_path = value %}
    <img src="{{ image_preview_path|default(v) }}?{{ random() }}" style="max-height: 100px;"/>
{% endblock %}